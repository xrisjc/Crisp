
writeLn('Welcome to Crisp REPL', 10);

let makeCounter = fn(start) {
    fn() {
        let result = start;
        start := start + 1;
        result
    }
};

let doStackOverflow = fn(x) {
    writeLn(x);
    doStackOverflow(x+1)
}