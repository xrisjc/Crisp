
writeLn('Welcome to Crisp REPL');

let makeCounter := fn(start) {
    fn() {
        let result := start;
        start := start + 1;
        result
    }
};

let doStackOverflow := fn(x) {
    writeLn(x);
    doStackOverflow(x+1)
}