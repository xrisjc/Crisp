{
    fn sumTo(n) {
        let count := 0
        let sum := 0
        while count = n = false
            sum := sum + (count := count + 1)
        sum
    }

    writeLn('the sum is ', sumTo(10))
}